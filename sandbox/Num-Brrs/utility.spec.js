// utility.spec.js
import {it, expect} from 'vitest';

it('should be green', () => { }); // TODO: remove
it('should be a good demo', () => {
    // This is the body of the test
    expect(theDemo).toBe('Working');
});